## Packages
date-fns | Date formatting and manipulation (essential for assistance dates)
framer-motion | Smooth animations for modals and page transitions
clsx | Utility for constructing className strings conditionally (often used with tailwind-merge)
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Inter'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
